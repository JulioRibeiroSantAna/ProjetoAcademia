# Configurações do Banco de Dados
DB_HOST=localhost
DB_NAME=sistema_nutricao
DB_USER=user
DB_PASS=password

# Configurações Docker (não alterar)
# O sistema detecta automaticamente se está rodando em Docker
