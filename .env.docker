# Configurações de Portas do Docker
# Altere aqui se alguma porta já estiver em uso no seu sistema

# Porta do site (padrão: 8080)
WEB_PORT=8080

# Porta do MySQL (padrão: 3306)
MYSQL_PORT=3306

# Porta do phpMyAdmin (padrão: 8081)
# Se a porta 8081 já estiver em uso, mude para outra (ex: 8082, 9090, etc)
PHPMYADMIN_PORT=8081
